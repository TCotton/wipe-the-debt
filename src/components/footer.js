import { Link } from "gatsby"
import React from "react"

const Footer = ({ siteTitle }) => (
  <footer>
    <p className={'footer'}>Havering Covid-19 Mutual Aid</p>
  </footer>
)
export default Footer;
